To reproduce the experiments in MAPR-R1 on trip-Advisor data with rare feature selection and the logistic regression model,
use runManyRareFeatures.py.

Select lambda, select which algos to run etc. 

The results are stored in saved_results_log_*

To reproduce the plots, use plot_results.py. 
